<ui:FluentWindow x:Class="BetterGenshinImpact.View.Dialogs.RoomBrowserDialog"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
                 Title="在线房间浏览器"
                 Width="450" Height="350"
                 WindowStartupLocation="CenterOwner"
                 WindowBackdropType="Auto"
                 ExtendsContentIntoTitleBar="True"
                 ui:Design.Background="{DynamicResource ApplicationBackgroundBrush}"
                 ui:Design.Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                 FontFamily="{DynamicResource TextThemeFontFamily}">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ui:TitleBar Grid.Row="0" Title="在线房间浏览器" />

        <ListView Grid.Row="1" x:Name="RoomListView" Margin="0,8,0,8"
                  SelectionMode="Single" MouseDoubleClick="OnRoomDoubleClick">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Center" />
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="房间码" Width="90" DisplayMemberBinding="{Binding Code}" />
                    <GridViewColumn Header="房主" Width="100" DisplayMemberBinding="{Binding HostName}" />
                    <GridViewColumn Header="UID" Width="100" DisplayMemberBinding="{Binding HostUid}" />
                    <GridViewColumn Header="人数" Width="50" DisplayMemberBinding="{Binding PlayerCount}" />
                    <GridViewColumn Header="期望" Width="50" DisplayMemberBinding="{Binding ExpectedPlayerCount}" />
                </GridView>
            </ListView.View>
        </ListView>

        <TextBlock Grid.Row="1" x:Name="EmptyText" Text="当前没有在线房间"
                   HorizontalAlignment="Center" VerticalAlignment="Center"
                   Foreground="{DynamicResource TextFillColorTertiaryBrush}"
                   Visibility="Collapsed" />

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <ui:Button Content="刷新" Margin="0,0,8,0" Click="OnRefreshClick" />
            <ui:Button Content="加入选中房间" Appearance="Primary" Click="OnJoinClick" />
        </StackPanel>
    </Grid>
</ui:FluentWindow>
